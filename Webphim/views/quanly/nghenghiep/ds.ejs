<% include ../layout %>
<div class="container-fluid">
    <div class="card">
        <div class="card-header">
            <h2>Danh sách nghề nghiệp</h2>
        </div>
        <div class="card-body">
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th>STT</th>
                        <th>Tên nghề nghiệp</th>
                        <th>Sửa</th>
                        <th>Xóa</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(i=0 ; i<data.length ; i++) { %>
                    <tr>
                        <td><%=i+1%></td>
                        <td><%=data[i].TenNgheNghiep%></td>
                        <td><a href="/admin/nghenghiep/sua/<%-data[i].ID_NgheNghiep%>">Sửa</a></td>
                        <td><a onclick="return Del('<%-data[i].TenNgheNghiep%>')" href="/admin/nghenghiep/xoa/<%-data[i].ID_NgheNghiep%>">Xóa</a></td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
                <a class="btn btn-primary" href="/admin/nghenghiep/them">Thêm nghề nghiệp</a>
        </div>
    </div>
</div>

<script>
    function Del(name)
    {
        return confirm("Bạn có chắc chắn muốn xóa nghề nghiệp:" + name +"?");
    }
</script>